<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:id="@+id/communityHeader"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@+id/communitySelector"
            app:layout_constraintHeight_default="percent"
            app:layout_constraintHeight_percent="@dimen/community_header_height"
            android:background="@color/grey">

        </FrameLayout>

        <FrameLayout
            android:id="@+id/communitySelector"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@+id/communityDisplay"
            app:layout_constraintHeight_default="percent"
            app:layout_constraintHeight_percent="@dimen/community_selector_height"
            android:background="@color/purple_rudder">

        </FrameLayout>

        <FrameLayout
            android:id="@+id/communityDisplay"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHeight_default="percent"
            app:layout_constraintHeight_percent="@dimen/community_display_height"
            android:background="@color/white">

        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
